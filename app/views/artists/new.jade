extends ../shared/template

block content
  h1.bannertwo New Artist
  form.form(method='POST', action='/artists', enctype='multipart/form-data')
    input(type='text', name='name', placeholder='New Artist Name')
    input(type='file', name='photo', multiple=false)
    button.addstuff New Artist
