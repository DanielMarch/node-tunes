extends ../shared/template

block content
  h1 Artists
  a(href='/artists/new') New Artist

  each artist in artists
    .artists
      .artistart: img(src='/img/#{artist.name}/#{artist.photo}')
      .artistname= artist.name
